__pycache__/
*.pyc
.venv/
venv/
*.json.bak
.DS_Store
.env
.env.bot1
.env.bot2
.env.minecraft
.env.test
.env.*
!.env.example
.panel.env
config.json

# Local runtime data (databases, event state, cross-instance snapshots).
# Paths are env-driven (BOT_DATA_DIR / PROJECT_STATS_DIR / EVENT_DATA_DIR);
# local env files point these under runtime/. Legacy root paths kept ignored
# so a default-path run never accidentally commits data.
runtime/
event_data/
database/
database-bot1/
database-bot2/
database-minecraft/
project_stats/

# Java build and local plugin runtime output.
minecraft-bridge/.gradle/
minecraft-bridge/build/
minecraft-bridge/run/

# AI agent local overrides (personal, not shared)
CLAUDE.local.md
.claude/settings.local.json
